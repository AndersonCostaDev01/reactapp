import styled from "styled-components";

export const FotoPerfil = styled.img `
  width: 128px;
  height: auto;
  border-radius: 40%;
  margin-bottom: 24px;
  box-shadow: 6px 6px 6px ${props => props.theme.corPrincipal};
`
